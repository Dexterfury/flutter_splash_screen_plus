import 'package:flutter/material.dart';

class MenGalleryScreen extends StatelessWidget {
  const MenGalleryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Men Gallery'),
      ),
    );
  }
}
